# 変更履歴 / Changelog

## v12.3.1 (2026-06)

### 表示色をHSLスライダーで細かく調整
- 色相（0–360）／彩度／明度の3スライダーを追加し、色を無段階で微調整可能に
  - 色相スライダーは虹色グラデ、彩度・明度スライダーは現在の色に応じた動的グラデで直感的
- カラーピッカー・プリセットスウォッチ・スライダーが相互に同期
- 依存ライブラリなし（単一HTMLのまま）。色はプレビュー表示のみでSTLには影響しない点は不変

## v12.3.0 (2026-06)

### モデル表示色の変更機能を追加（カラーフィラメントのシミュレーション）
- ブラウザのプレビュー上でモデルの色を自由に変更できるカラーピッカーを追加
- よく使う色のプリセット（スウォッチ）も用意（ダークグレー／赤／青／緑／黄／白）
- 色はプレビュー表示のみに反映（バイナリSTLは色情報を持たないため、ジオメトリやエクスポートには影響せず、再生成も走らない）

## v12.2.2 (2026-06)

### 水密STLの形状破綻を修正
- 水密生成でビーム（円柱）の配置に使っていた手組みの変換行列が、Manifoldの行列レイアウト解釈と食い違っており、**円柱が誤った長さ・向きに化けてジオメトリが放射状に破裂**していた不具合を修正
  - 原因：`Manifold.transform()` に渡す3×4行列の列/行の解釈ずれ
  - 対策：行列を手組みせず、Manifold標準の `rotate`（原点回りの単軸回転）＋`translate` で配置。極角で+zから傾け、方位角で回し、基点へ平行移動
- 出力の直径が正しく実寸（例：D50→約50mm）になることを確認。バウンディングボックスもNodeでD一致を検証
- ノード球の分割数を16→8に変更（微小な節点には十分で、三角形数とファイルサイズを削減）
- 結果：同条件のSTLが約7.8MB（破裂状態）→約1.7MBに縮小、かつ形状が正常化

## v12.2.1 (2026-06)

### 水密STL出力の互換性修正
- 水密生成の実行方法を「Blob製モジュールWorker＋CDNからのcross-origin import」から、**メインスレッドでの動的 `import()`** に変更
  - モバイル／アプリ内ブラウザ（iOS Safari系）でモジュールWorkerのcross-origin importが失敗し、常にフォールバックしていた問題を修正
  - 対象のレベル0〜2ではunionが数msと軽いため、メインスレッド実行でも体感ブロックは無い
- 失敗時は従来どおり重ね合わせ方式へ自動フォールバック（STLは必ず出力される、は不変）

## v12.2.0 (2026-06)

### 水密（マニフォールド）STL出力の追加
- STL出力時に、ビーム＝円柱・節点＝球をブーリアン和して**真に水密な単一ソリッド**を生成する経路を追加（Manifold WASM ライブラリを使用）
  - 対象範囲：**三角・六角パターンともレベル2まで**（この範囲ではブーリアン和が高速・安定・決定的で、実測で水密性を確認済み。レベル3以上はManifoldの厳密演算が非決定的に暴走しうるため対象外）
  - 範囲外（レベル3以上）は、従来の重ね合わせ方式で即時出力（多くのスライサーで印刷可能）。待ち時間は発生しない
- **STLは必ず出力される設計**：水密生成が失敗・タイムアウト・CDN未到達の場合は自動で従来方式にフォールバック
- 水密生成は専用の**モジュールWorker**で実行し、想定外に重い場合はタイムアウトで強制終了して安全にフォールバック（ページが固まらない）
- 水密で出力したファイルは名前末尾に `_manifold` を付与。ステータスに「完了（水密）」を表示
- プレビュー表示は従来どおり高速な重ね合わせメッシュのまま（操作性を維持）

## v12.1.0 (2026-06)

### 不具合修正・改善
- **タッチ操作を実装**（v12.0.0のCHANGELOGに記載されていたが未実装だった点を修正）
  - 1本指ドラッグで回転、2本指ピンチでズーム、2本指ドラッグでパン
  - canvasに `touch-action:none` を付与し、ブラウザのスクロール/ズームと競合しないように
- **CDNフォールバックを追加**：主CDN（cdnjs）が読み込めない場合は unpkg から Three.js を取得。両方失敗してもSTL出力は動作するようガード
- **重いメッシュ生成をWeb Worker化**：高い分割レベルでもUIがフリーズしないよう、ジオメトリ計算をワーカースレッドへ分離（Worker不可の環境では同期フォールバック）。生成中はステータスに「生成中 / Generating...」を表示
- **プレビュー更新時に視点がリセットされる問題を修正**：初回読み込み時のみ画角合わせを行い、以降の更新では現在の視点を維持
- インデックス型の判定境界を精密化（`> 65535` → `> 65536`。Uint16は頂点0〜65535を参照可能）

## v12.0.0 (2026-06)

### 配色・デザイン
- ページ全体の配色を白黒ベースに変更
- 3Dモデルの色を黒に近いグレー (#3a3a3a) に変更
- ボタンの色を黒 (#111827) に変更
- 形状選択（ホールパターン・断面形状）のハイライト色を青から黒に変更
- ステータス表示色を新しい配色に合わせて調整

### コード品質改善（GitHub Copilotレビュー反映・1回目）
- 入力値バリデーションを追加（`getParams()`）
  - 直径・枠の太さ・形状分割レベルの範囲チェック
- try/catchによるエラーハンドリングを追加（`safeUpdate()`）
  - エラー発生時にステータス表示でメッセージを通知
- STLダウンロード処理をBlobベースに変更（base64データURLから移行）
- 変数名 `sub` を `subd` にリネーム（グローバル関数とのシャドーイング解消）
- 未使用の `b64` 関数を削除
- ステータス表示に `aria-live="polite"` を追加（アクセシビリティ向上）

### コード品質改善（GitHub Copilotレビュー反映・2回目）
- `preserveDrawingBuffer` を `false` に変更（描画の軽量化）
- `keyLight.target` の追加処理を初期化時の一度だけに整理（再描画ごとの重複追加を解消）
- タッチ操作対応を追加
  - スマートフォン・タブレットで1本指ドラッグによる回転操作
  - 2本指ピンチ操作によるズーム

---

## それ以前

- Netlifyからの移行、GitHub Pagesでの公開
- 2D canvasワイヤーフレーム表示から、Three.jsによる3Dソリッドビューアへ刷新
