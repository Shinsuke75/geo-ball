geo-ball v12.1.0（Triangle & Hexagon）
=====================================
- 四角（Square）は穴の形状からは分離済み。穴の形状は三角・六角のみ（断面形状は三角・四角・円）。
- どちらも面接地になるように向きを自動調整 → 出力前にz=0フロアリング＋微小負値クランプ。
- 退化三角形は除去してクリーンなSTLを生成。
- メッシュ生成はWeb Workerで実行（高分割でもUIが固まらない／非対応環境では同期実行）。
- マウス・タッチ両対応（1本指で回転、2本指でピンチズーム／パン）。
- Three.js は cdnjs を主、unpkg を予備のCDNとして読み込み（両方失敗してもSTL出力は可能）。

デプロイ方法（Netlify）
-----------------------
1. このZIPを解凍して、`index.html` を Netlify の Drag & Drop に渡すか、
   リポジトリに `index.html` を追加してデプロイしてください。
2. Node バージョン固定が必要なら、プロジェクト側で `package.json` の engines.node か
   `netlify.toml` の NODE_VERSION を設定してください。

更新日時: 2026-06-22
